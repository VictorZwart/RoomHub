{% extends "generic.twig" %}

{% block title %}Room Overview{% endblock %}
{% block page_stylesheet %}
    <link rel="stylesheet" href="{{ static('css/rooms.css') }}" type="text/css">
{% endblock %}
{% block content %}
{#    just for styling, needs to be created with php#}

    <div class="row">
        <div class="col-sm-6">
            <div class="card">
                <img src="{{ static('images/room_placeholder.jpg') }}" class="card-img-top" alt="Pic of room">
                <div class="card-body">
                    <h5 class="card-title">Location</h5>
                    <p class="card-text">More details provided in the room description by the tenant.</p>
                    <a href="#"><button class="square_button">More details</button></a>
                </div>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="card">
                <img src="{{ static('images/room_placeholder.jpg') }}" class="card-img-top" alt="Pic of room">
                <div class="card-body">
                    <h5 class="card-title">Location</h5>
                    <p class="card-text">More details provided in the room description by the tenant.</p>
                    <a href="#"><button class="square_button">More details</button></a>
                </div>
            </div>
        </div>
    </div>
    {#    just for styling, needs tobe created with php#}

    <ul>
        {% for room in all_rooms %}
            <li><a href="{{ url('rooms/' ~ room.room_id) }}">GOTO ROOM</a> {{ room }}</li>
        {% endfor %}
    </ul>
{% endblock %}

