{% extends "generic.twig" %}

{% block title %}Room Overview{% endblock %}
{% block content %}
    Alle rooms hier

    <ul>
        {% for room in all_rooms %}
            <li><a href="{{ base() }}/rooms/{{ room.room_id }}">GOTO ROOM</a> {{ room }}</li>
        {% endfor %}
    </ul>
{% endblock %}

